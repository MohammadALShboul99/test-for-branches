import 'package:flutter/material.dart';

class SeconeScreen extends StatelessWidget {
  const SeconeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: Text("12345")),
    );
  }
}
